<div class="newinv-background">
  <div class="newevent-content form-content">
    <h1>Invitation for Event <%= @event.title %></h1>

    <%= render "form", invitation: @invitation, event: @event %>
    <div>
      <%= link_to "Back to Event", event_path(@event) %>
    </div>
  </div>
</div>
